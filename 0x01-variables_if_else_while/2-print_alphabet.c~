#include <stdio.h>
#include <conio.h>
/**
*main - prints the alphabet in lower case.
*Return: while loop.
*/

int main(void)
{

int putchar (int c);
clrscr();

for (c == 'a'; c <= 'z'; c++)

putchar('\n');

return (0);
}
