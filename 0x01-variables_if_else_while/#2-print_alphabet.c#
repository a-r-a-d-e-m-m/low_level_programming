#include <stdio.h>
/**
*main - prints the alphabet in lower case.
*Return: while loop.
*/

int main(void)
{
int c='Z';
 while(c = 'a', c <= 'z', c++)
putchar(c);

else (c <= 'z'; c++)
putchar(c,'\n');
return (0);
}
